# Supabase — Project Settings → API
VITE_SUPABASE_URL=https://gfmxznmnclbsdenozrxd.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# ── Local dev only ──────────────────────────────────────────────
# VITE_BORG_AI_LOCAL=true
# VITE_BORG_AI_MODEL=llama3.2:3b
# VITE_OLLAMA_URL=http://127.0.0.1:11434
# Test the production AI path from localhost (Supabase edge → Hetzner):
# VITE_BORG_AI_USE_REMOTE=true

# ── Production (Netlify env vars) ───────────────────────────────
# VITE_SUPABASE_URL
# VITE_SUPABASE_ANON_KEY
# Do NOT set VITE_BORG_AI_LOCAL or VITE_OLLAMA_URL in production.

# BORG AI daily limit — run supabase/borg-ai-daily-limit.sql in SQL editor

# Production AI (Supabase secrets + Hetzner Ollama):
#   1. DNS: ai.borgwithus.xyz → Hetzner IPv4 (Netlify DNS)
#   2. Hetzner: Ollama on 127.0.0.1:11434 + Caddy HTTPS reverse proxy
#   3. npx supabase secrets set AI_INFERENCE_API_HOST=https://ai.borgwithus.xyz
#   4. npx supabase secrets set BORG_AI_MODEL=llama3.2:3b
#   5. On Hetzner: bash scripts/hetzner-ollama-step1.sh
#   6. npm run supabase:deploy-ai
#   7. Supabase Auth → Site URL https://borgwithus.xyz, redirects https://borgwithus.xyz/**
